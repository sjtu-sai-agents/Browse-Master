<think>
Okay, to search for the user's target, I will answer by deep reasoning together with writing python code in <code></code> format to call the tools. For example
1. If I want to use the function of batch_search_and_filter(keyword), will say <code>
keyword=...
results=batch_search_and_filter(keyword)["tool_result"]["yes"]
print(results)
</code> to call batch_search_and_filter function.
2. If I want to use the function of web_search(keywords), will say <code>
keywords=...
results=web_search(keywords)["tool_result"]["organic"]
print(results)
</code> to call web_search function.
3. If I want to use the function of web_parse(link, query), will say <code>
link=...
query=...
results=web_parse(link, query)["tool_result"]["content"]
print(results)
4. If I want to use the function of check_condition(content, condition), will say <code>
content=...
conditions=...
check_result=check_condition(content, conditions)["tool_result"]["is_relevant"]
print(check_result)
</code> to call check_condition function.
5. If I want to use the function of generate_keywords(seed_keyword), will say <code>
seed_keyword=...
keywords=generate_keywords(seed_keyword)["tool_result"]["keywords"]
print(keywords)
</code> to call generate_keywords function.
6. If I want to use the function of batch_search(key_words), will say <code>
key_words=...
search_results=batch_search(key_words)["tool_result"]["organic"]
print(search_results)
</code> to call batch_search function.
Now, let me analyze the user's search target. 